<header class="navbar navbar-fixed-top">
  <div class="container">
    <% if logged_in? %>
    <%= link_to "sample app", microposts_path, id: "logo" %>
    <nav>
      <ul class="nav navbar-nav navbar-right" >
        <li><%= link_to "Top", microposts_path, id:"list" %></li>
        <li><%= link_to "Users", users_path, id:"list" %></li>
        <li><%= link_to "Profile", current_user, id:"list" %></li>
        <li><%= link_to "Logout", logout_path, method: :delete, id:"list" %></li>
      </ul>
    </nav>
    <% else %>
      <%= link_to "sample app", root_url, id: "logo" %>
    <% end %>
  </div>
</header>